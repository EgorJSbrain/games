export const ROUTES = {
  home: '/',
  login: '/login',
  games: '/games',
}

export enum ButtonColorType {
  grey = 'grey',
  green = 'green',
}

export enum ResponseStatuses {
  success = 'success',
  fail = 'fail',
};

export enum AlertTypes {
  success = 'success',
  error = 'error',
  warning = 'warning',
};

export const STORAGE_TOKEN_NAME = 'authToken';
